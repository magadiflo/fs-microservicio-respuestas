spring.application.name=microservicio-respuestas
server.port=${PORT:0}
eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}
eureka.client.service-url.defaultZone=http://127.0.0.1:8761/eureka

spring.datasource.url=jdbc:mysql://127.0.0.1:3306/bd_fs_microservicios_examenes
spring.datasource.username=admin
spring.datasource.password=magadiflo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Permitimos que genere de forma automática el esquema ddl (Se creen las tablas automáticamente en la BD)
# En esta actualización establecemos en false para que no cree la tabla alumnos, ya que (1) el Entity Alumno
# está dentro del class Path de la clase principal en el EntityScan [Pero como no lo usaremos lo eliminamos], 
# (2) la tabla alumnos ya lo tenemos en la bd postresql
spring.jpa.generate-ddl=false

